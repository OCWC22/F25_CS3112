================================================================================
SECTION 22.1: BELLMAN-FORD ALGORITHM
================================================================================

--------------------------------------------------------------------------------
PART 1: (DEFINITIONS & CONCEPTS)
--------------------------------------------------------------------------------
1. **Shortest Path:** The path between two nodes with the minimum total sum of edge weights.
2. **Relaxation:** The core operation of shortest path algorithms.
   - *Equation:* `if d[v] > d[u] + w(u,v) then d[v] = d[u] + w(u,v)`
   - *Translation:* "I currently think it takes `d[v]` cost to get to `v`. But I just found a path through `u` that costs `d[u] + w(u,v)`. If that's cheaper, I'll update my belief."
   - *Analogy:* Checking Google Maps and finding a faster route.
3. **Negative Weight Edge:** An edge that *reduces* the total cost (e.g., a rebate or time gain).
   - *Danger:* Standard algorithms like Dijkstra crash here.
4. **Negative Weight Cycle:** A loop where the sum of weights is negative.
   - *Result:* You can spin in this loop forever, reducing cost to -Infinity. Shortest path is undefined.
5. **Convergence:** When the algorithm stops updating values because the optimal solution has been found.

--------------------------------------------------------------------------------
PART 2: HOMEWORK SOLUTIONS (STEP-BY-STEP)
--------------------------------------------------------------------------------

### 22.1-1
**Problem:** Run Bellman-Ford on Figure 22.4 using vertex z as the source, then modify edge (z,x) to weight 4 and run again using s as source.

**Figure 22.4 Graph (reconstructed from problem):**
```
Original Graph:
          6       5
    s ------> t ------> y
    ^         ^       ^
    |         |       |
   -1|        |-2     |-3
    |         |       |
    |         |       |
    x <------ z ------> y
          7           8
```

**Execution 1: Source = z**

**Initial state (Pass 0):**
```
d[s] = ∞, d[t] = ∞, d[x] = ∞, d[y] = ∞, d[z] = 0
π[s] = NIL, π[t] = NIL, π[x] = NIL, π[y] = NIL, π[z] = NIL
```

**Pass 1: Relax all edges**
```
Edge (z,x): d[x] = min(∞, 0+7) = 7, π[x] = z
Edge (z,y): d[y] = min(∞, 0+8) = 8, π[y] = z
Edge (x,t): d[t] = min(∞, 7+5) = 12, π[t] = x
Edge (t,y): d[y] = min(8, 12+2) = 8, π[y] = z
Edge (t,s): d[s] = min(∞, 12+1) = 13, π[s] = t
Edge (s,t): d[t] = min(12, 13+6) = 12, π[t] = x

After Pass 1:
d = [13, 12, 7, 8, 0]
π = [t, x, z, z, NIL]
```

**Pass 2: Relax all edges again**
```
Edge (z,x): d[x] = min(7, 0+7) = 7 (no change)
Edge (z,y): d[y] = min(8, 0+8) = 8 (no change)
Edge (x,t): d[t] = min(12, 7+5) = 12 (no change)
Edge (t,y): d[y] = min(8, 12+2) = 8 (no change)
Edge (t,s): d[s] = min(13, 12+1) = 13 (no change)
Edge (s,t): d[t] = min(12, 13+6) = 12 (no change)

After Pass 2: No changes - algorithm has converged!
```

**Execution 2: Modified (z,x) = 4, Source = s**

**Modified Graph:**
```
          6       5
    s ------> t ------> y
    ^         ^       ^
    |         |       |
   -1|        |-2     |-3
    |         |       |
    |         |       |
    x <------ z ------> y
          4           8
```

**Initial state (Pass 0):**
```
d[s] = 0, d[t] = ∞, d[x] = ∞, d[y] = ∞, d[z] = ∞
π[s] = NIL, π[t] = NIL, π[x] = NIL, π[y] = NIL, π[z] = NIL
```

**Pass 1: Relax all edges**
```
Edge (s,t): d[t] = min(∞, 0+6) = 6, π[t] = s
Edge (t,x): d[x] = min(∞, 6+5) = 11, π[x] = t
Edge (x,z): d[z] = min(∞, 11-1) = 10, π[z] = x
Edge (z,y): d[y] = min(∞, 10+8) = 18, π[y] = z
Edge (t,y): d[y] = min(18, 6+2) = 8, π[y] = t

After Pass 1:
d = [0, 6, 11, 8, 10]
π = [NIL, s, t, t, x]
```

**Pass 2: Continue relaxing**
```
Edge (x,z): d[z] = min(10, 11-1) = 10 (no change)
Edge (z,x): d[x] = min(11, 10+4) = 11 (no change)
Other edges also stable

After Pass 2: No further changes
```

**Final Result:** Algorithm converges with optimal shortest paths found.

**Real Engineering Application:**
*   **Scenario:** **Distance Vector Routing (RIP Protocol).**
    - Old internet routers used this exact algorithm.
    - Router A tells Router B: "I can reach Google in 5 hops."
    - Router B tells Router C: "I can reach Google in 6 hops (via A)."
    - This "gossip" spreads periodically. If a link dies (weight Infinity), the bad news propagates slowly (Pass by Pass). This slow convergence is why modern internet uses OSPF (Dijkstra) instead.

---

### 22.1-2
**Problem:** Prove that a path exists from s to v IFF Bellman-Ford terminates with v.d < Infinity.

**Visual Proof:**

**Part 1: Path exists → v.d < ∞**

**Graph example:**
```
Graph example:
    s → a → b → v
    1   2   3

If there's a path s→a→b→v with finite weights:
```

**Step-by-step propagation:**
```
Pass 1: s relaxes a → d[a] = 1
    s(0) → a(1) → b(∞) → v(∞)
```

```
Pass 2: a relaxes b → d[b] = 1+2 = 3
    s(0) → a(1) → b(3) → v(∞)
```

```
Pass 3: b relaxes v → d[v] = 3+3 = 6
    s(0) → a(1) → b(3) → v(6)

Result: v.d = 6 < ∞ ✓
```

**Part 2: v.d < ∞ → Path exists**

**If v.d = 5 at termination:**
```
This means some edge (u,v) was relaxed:
d[v] = d[u] + w(u,v) = 5

But d[u] = 3, so there must be edge (w,u):
d[u] = d[w] + w(w,u) = 3

And d[w] = 1, so there must be edge (s,w):
d[w] = d[s] + w(s,w) = 1

But d[s] = 0 (source!)
```

**Chain back:**
```
s(0) → w(1) → u(3) → v(5)
    ↑       ↑       ↑
   source   edge    edge

Complete path: s → w → u → v
A complete path exists! ✓
```

**Conclusion:** Finite distance implies traceable path back to source.

**LeetCode Pattern:**
*   **Problem:** "Cheapest Flights Within K Stops" (Medium).
*   **Pattern:** This problem is literally Bellman-Ford but limited to `K` passes instead of `|V|-1`. It asks "Can I get there?" (Reachability) and "What's the cost?" (Shortest Path).

---

### 22.1-6
**Problem:** Find the shortest path to `v` from ANY source `u` in the graph. (Compute min_{u} delta(u, v)).

**The Challenge:**
- Running Bellman-Ford V times (once for each start node) is too slow (O(V^2 E)).
- We need O(VE).

**The "Super Source" Solution:**

**Original Problem:**
```
Find min distance to v from ANY source u:

      5          2
  A ------ B ------ C
  |        |        |
  |3       |1       |4
  |        |        |
  D ------ E ------ F
      6          1

For each vertex v, we want: min(dist(A,v), dist(B,v), dist(C,v), dist(D,v), dist(E,v), dist(F,v))
```

**Solution: Add Super Source S'**
```
           S'
         / | \
        /  |  \
       0   0   0
      /    |    \
     v     v     v

    A ---- B ---- C
    |      |      |
    |3     |1     |4
    |      |      |
    D ---- E ---- F
        6      1

Super source S' connects to EVERY vertex with weight 0
```

**Algorithm Analysis:**
```
Run Bellman-Ford from S':

- Initial: d[S'] = 0, all others = ∞
- After first pass: All vertices get d = 0 (via S' edges)
- Continue V-2 more passes: Finds all shortest paths

For any vertex v:
d[v] = min_u δ(S',u) + δ(u,v) = min_u 0 + δ(u,v) = min_u δ(u,v) = δ*(v)

But wait... δ(v,v) = 0, so answer is always 0 unless negative cycles!
```

**Real Solution - Negative Cycle Detection:**
```
Actually, the question is about detecting negative cycle reachability:
- If v can be reached by negative cycle: δ*(v) = -∞
- Otherwise: δ*(v) = 0 (since you can start at v itself)

Algorithm:
1. Run standard Bellman-Ford from any vertex to detect negative cycles
2. Run DFS/BFS from all negative cycle vertices
3. For vertices reachable from negative cycles: δ*(v) = -∞
4. For all other vertices: δ*(v) = 0

Time: O(VE) for Bellman-Ford + O(V+E) for reachability = O(VE)
```

**Real Engineering Application:**
*   **Scenario:** **Supply Chain Optimization.**
    - You have 50 factories (u) and 1 customer (v).
    - You want to find which factory can ship to the customer cheapest.
    - Instead of calculating 50 separate paths, you reverse the map arrows.
    - You start at the Customer and find the shortest path to *all* factories in one go.
    - The factory with the lowest result is your winner.
