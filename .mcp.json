{
  "mcpServers": {
    "pdf-reader-mcp": {
      "command": "docker",
      "args": ["run", "-i", "--rm", "-v", "$PWD:/pdfs", "trafflux/pdf-reader-mcp"],
      "name": "PDF Reader (Docker)"
    }
  }
} 